-- UPDDATE

-- Esta query se encarga de editar o actualizar un registro en nuestra DB

-- Es importante que el UPDATE siempre este acompañado de un WHERE
UPDATE Rectangulos 
SET BASE = 20, ALTURA = 30
WHERE ID = 5;

